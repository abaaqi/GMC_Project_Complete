# Netlify config for the MahaFarm frontend (SPA).
# Set VITE_API_BASE_URL and VITE_ENABLE_MOCK_DATA in Site settings → Environment.

[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
